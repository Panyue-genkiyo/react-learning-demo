import React from 'react';

const SearchForm = () => {
    return (
        <div className='search_form'>
            <form>
                <input type='text'/>
                <button>Search</button>
            </form>
        </div>
    );
};

export default SearchForm;
